## Introduction

This node makes HTTP request with methods like GET, POST, PATCH, PUT and DELETE then returns the response. It can be used to interact with RESTful APIs, fetch data, or send data to a server. It supports custom headers and we can also send body to the request.
